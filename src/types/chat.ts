export interface ChatMessage {
  id: string
  fromUserId: string
  fromProfileId: string
  toProfileId?: str
  timestamp: string
  type: 'user-to-

  id: string
  lastMessage: ChatMessage
 

export interface ChatRequest {
  fromUserId
  toUserId: string
  status: 'pending' | 'acc
}













