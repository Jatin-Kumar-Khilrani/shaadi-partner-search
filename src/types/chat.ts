export interface ChatMessage {
  id: string
  toProfileId?: stri
  timestamp: string
  type?: 'user' | 'ad

  id: string
  lastMessage: 
  createdAt: string
}

  fromUserId: string
  toUserId: 
  status: 'pending' | 'a
}













