export interface ChatMessage {
  fromProfil
  fromProfileId: string
  toProfileId: string
  timestamp: stri
  type: 'user' | 'admin'
  toUserId?: string
  participants:
  timestamp?: string

  createdAt: string
  updatedAt?: string
}

  timestamp: string
  unreadCount: number
  createdAt: stringg
}

  createdAt: string
}